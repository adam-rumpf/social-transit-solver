/**
Objective function calculation.

Includes a variety of functions for calculating the objective function value for a given solution. This requires using the network object to calculate graph distances, and then using those distances to calculate gravity-based accessibility metrics.
*/

#pragma once
